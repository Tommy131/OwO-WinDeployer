# .NET build output
bin/
obj/
.vs/
*.user

# WinDeploy artifacts
artifacts/
publish/
dist/

# 离线安装包 / 下载的大体积二进制（如 VMware .7z），不入库
assets/packages/

# 安全：配置仓库中绝不提交任何私钥
configs/ssh/id_*
configs/ssh/*.pem
configs/ssh/*.key

# 安全/个性化：不提交个人的 git 与 VS Code 配置（由各自机器通过「配置同步」导出生成）
configs/git/.gitconfig
configs/vscode/settings.json
configs/vscode/keybindings.json
configs/vscode/extensions.txt
