<Window x:Class="WinDeploy.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="700" Width="1000"
        MinHeight="560" MinWidth="820"
        Background="{DynamicResource PageBg}"
        WindowStartupLocation="CenterScreen">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="200" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <!-- Left nav rail: collapsible grouped sections -->
    <Border Grid.Column="0" Background="{DynamicResource NavBg}" BorderBrush="{DynamicResource BorderSoft}" BorderThickness="0,0,1,0">
      <DockPanel Margin="8,16,8,10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8,0,0,14">
          <TextBlock Text="&#xE950;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                     Foreground="{DynamicResource Accent}" VerticalAlignment="Center" />
          <TextBlock Text="{Binding AppName}" FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0"
                     VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding WindowTitle}" />
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Copyright}" FontSize="10"
                   Foreground="{DynamicResource TextTertiary}" Margin="8,8,4,2" TextWrapping="Wrap"
                   ToolTip="{Binding WindowTitle}" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding NavGroups}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <!-- one group -->
                <StackPanel Margin="0,0,0,2" Visibility="{Binding HasVisibleItems, Converter={StaticResource BoolToVis}}">

                  <!-- group header (click to collapse / expand) -->
                  <Button Command="{Binding ToggleCommand}" Cursor="Hand" Margin="0,6,0,1"
                          HorizontalContentAlignment="Stretch">
                    <Button.Template>
                      <ControlTemplate TargetType="Button">
                        <Border x:Name="Hd" Background="Transparent" CornerRadius="6" Padding="7,5">
                          <DockPanel>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding ChevronGlyph}" FontFamily="Segoe MDL2 Assets"
                                       FontSize="10" Foreground="{DynamicResource TextTertiary}" VerticalAlignment="Center" Margin="4,0,2,0" />
                            <TextBlock Text="{Binding Glyph}" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="22"
                                       Foreground="{DynamicResource Accent}" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Title}" FontSize="11.5" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextTertiary}" VerticalAlignment="Center" />
                          </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                          <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Hd" Property="Background" Value="{DynamicResource PageBg}" />
                          </Trigger>
                        </ControlTemplate.Triggers>
                      </ControlTemplate>
                    </Button.Template>
                  </Button>

                  <!-- group items (collapsible) -->
                  <ItemsControl ItemsSource="{Binding Items}"
                                Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Button Cursor="Hand" Margin="0,1,0,1" HorizontalContentAlignment="Stretch"
                                Visibility="{Binding IsVisible, Converter={StaticResource BoolToVis}}"
                                Command="{Binding DataContext.SelectNavCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}">
                          <Button.Template>
                            <ControlTemplate TargetType="Button">
                              <Border x:Name="Bd" CornerRadius="6" Padding="10,7" Margin="14,0,0,0" Background="Transparent">
                                <StackPanel Orientation="Horizontal">
                                  <TextBlock Text="{Binding Glyph}" FontFamily="Segoe MDL2 Assets" FontSize="14" Width="20"
                                             Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                                  <TextBlock Text="{Binding Label}" FontSize="13" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                                </StackPanel>
                              </Border>
                              <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                  <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PageBg}" />
                                </Trigger>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                  <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBg}" />
                                </DataTrigger>
                              </ControlTemplate.Triggers>
                            </ControlTemplate>
                          </Button.Template>
                        </Button>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </DockPanel>
    </Border>

    <!-- Content -->
    <ContentControl Grid.Column="1" Content="{Binding Current}" Margin="0" />
  </Grid>
</Window>
