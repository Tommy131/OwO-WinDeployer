<UserControl x:Class="WinDeploy.App.Views.FtpServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- status + actions -->
    <Border Grid.Row="0" Style="{StaticResource Card}" Padding="16,13">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" VerticalAlignment="Center">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="状态：" FontSize="14" Foreground="{DynamicResource TextSecondary}" />
            <TextBlock Text="{Binding StatusText}" FontSize="14" FontWeight="SemiBold">
              <TextBlock.Style>
                <Style TargetType="TextBlock">
                  <Setter Property="Foreground" Value="{DynamicResource TextTertiary}" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding Running}" Value="True">
                      <Setter Property="Foreground" Value="{DynamicResource OkFg}" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </TextBlock.Style>
            </TextBlock>
            <TextBlock Text="{Binding UptimeText}" FontSize="12.5" Foreground="{DynamicResource TextTertiary}" Margin="12,0,0,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding ConnCountText}" FontSize="12.5" Foreground="{DynamicResource TextTertiary}" Margin="12,0,0,0" VerticalAlignment="Center" />
          </StackPanel>
          <TextBlock Text="{Binding EndpointText}" FontSize="12" FontFamily="Consolas" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0" TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="▶ 启动" Style="{StaticResource OkButton}" Margin="0,0,8,0" Command="{Binding StartCommand}" />
          <Button Content="■ 停止" Style="{StaticResource DangerButton}" Command="{Binding StopCommand}" />
        </StackPanel>
      </Grid>
    </Border>

    <!-- reachable addresses -->
    <Border Grid.Row="1" Style="{StaticResource Card}" Padding="16,11" Margin="0,12,0,0">
      <StackPanel>
        <TextBlock Text="可连接地址（客户端使用以下地址 + 控制端口登录）" FontSize="12" Foreground="{DynamicResource TextTertiary}" />
        <TextBlock Text="{Binding LocalAddresses}" FontSize="13" FontFamily="Consolas" Foreground="{DynamicResource TextPrimary}" Margin="0,5,0,0" TextWrapping="Wrap" />
      </StackPanel>
    </Border>

    <!-- connections -->
    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="2" Margin="0,12,0,0">
      <Grid>
        <TextBlock Text="暂无连接。启动服务端并等待客户端登录后，连接将显示在此处。"
                   FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="14,12,0,0" VerticalAlignment="Top"
                   Visibility="{Binding NoConnections, Converter={StaticResource BoolToVis}}" />
        <DataGrid ItemsSource="{Binding Connections}" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False" GridLinesVisibility="Horizontal"
                  Background="Transparent" Margin="6">
          <DataGrid.Columns>
            <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="40" />
            <DataGridTextColumn Header="来源" Binding="{Binding Remote}" Width="2*" />
            <DataGridTextColumn Header="用户" Binding="{Binding User}" Width="*" />
            <DataGridTextColumn Header="活动" Binding="{Binding Activity}" Width="*" />
            <DataGridTextColumn Header="时长" Binding="{Binding SinceText}" Width="Auto" />
            <DataGridTextColumn Header="传输" Binding="{Binding TransferText}" Width="Auto" />
          </DataGrid.Columns>
        </DataGrid>
      </Grid>
    </Border>

    <!-- log -->
    <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0" Margin="0,12,0,0" Height="160">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="12,8,12,4">
          <Button DockPanel.Dock="Right" Content="清空" Style="{StaticResource MiniButton}" Command="{Binding ClearLogCommand}" />
          <TextBlock Text="服务端日志" FontSize="12.5" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
        </DockPanel>
        <TextBox x:Name="LogBox" Grid.Row="1" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                 FontFamily="Consolas" FontSize="11.5" BorderThickness="0" Background="Transparent"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap" Margin="6,0,2,6" />
      </Grid>
    </Border>
  </Grid>
</UserControl>
