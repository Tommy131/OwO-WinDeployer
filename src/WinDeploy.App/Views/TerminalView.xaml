<UserControl x:Class="WinDeploy.App.Views.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="24,18,24,18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="终端" FontSize="20" FontWeight="SemiBold" />
      <TextBlock Text="内置 PowerShell / cmd，快速输入命令调试。会话状态会保留（cd、变量等），已切到 UTF-8 编码。" FontSize="13"
                 Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0" />
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
      <RadioButton Content="PowerShell" IsChecked="{Binding IsPowerShell}" GroupName="shell" VerticalAlignment="Center" />
      <RadioButton Content="cmd" IsChecked="{Binding IsCmd}" GroupName="shell" VerticalAlignment="Center" Margin="16,0,0,0" />
      <Button Content="清屏" Style="{StaticResource MiniButton}" Command="{Binding ClearCommand}" Margin="24,0,0,0" />
      <Button Content="重启" Style="{StaticResource MiniButton}" Command="{Binding RestartCommand}" Margin="8,0,0,0"
              ToolTip="结束并重新启动 shell（清空会话状态）" />
      <TextBlock Text="{Binding Status}" FontSize="12" Foreground="{DynamicResource TextTertiary}"
                 VerticalAlignment="Center" Margin="16,0,0,0" TextTrimming="CharacterEllipsis" />
    </StackPanel>

    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="2" Margin="0,12,0,0">
      <TextBox x:Name="OutBox" Text="{Binding Output, Mode=OneWay}" IsReadOnly="True"
               FontFamily="Consolas" FontSize="12.5" TextWrapping="Wrap" BorderThickness="0"
               Background="Transparent" VerticalContentAlignment="Top" Padding="10"
               VerticalScrollBarVisibility="Auto" TextChanged="OutBox_TextChanged" />
    </Border>

    <Grid Grid.Row="3" Margin="0,10,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox x:Name="InBox" Grid.Column="0" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}"
               FontFamily="Consolas" FontSize="13" Height="36" VerticalContentAlignment="Center" Padding="10,0">
        <TextBox.InputBindings>
          <KeyBinding Key="Enter" Command="{Binding SendCommand}" />
        </TextBox.InputBindings>
      </TextBox>
      <Button Grid.Column="1" Content="运行" Style="{StaticResource PrimaryButton}" Command="{Binding SendCommand}" Margin="8,0,0,0" />
    </Grid>
  </Grid>
</UserControl>
